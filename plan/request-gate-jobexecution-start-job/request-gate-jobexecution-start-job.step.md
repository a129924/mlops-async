## Implementation Steps

- [X] 凍結 `start_job` planning baseline，確認只指向 `jobExecution/jobRequests/jobs`
- [X] 確認四個 planning artifacts 的 exact paths、locked decisions 與 strict analysis routing 一致
- [X] 確認 future test directory 名稱固定為 `tests/unit/request_contract/job_requests_jobs_request_gate/`
- [X] 將 legacy source 人類確認記錄為 `human-confirmed external source evidence`，且排除 physical path
- [X] 本 topic 進入 planner final gate，不進入 implementation
